import { RecordMaker } from "@/components/builder/RecordMaker";

// Bulk 생활기록부 generation across a semester's assignments. Login/owner-gated
// inside the client component.

export default function RecordsPage() {
  return <RecordMaker />;
}
