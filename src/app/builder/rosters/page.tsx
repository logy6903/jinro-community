import { RosterManager } from "@/components/builder/RosterManager";

// Class roster (명렬) management. Login/owner-gated inside the client component.

export default function RostersPage() {
  return <RosterManager />;
}
