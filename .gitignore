# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
!.env.example

# Firebase service account key — full-admin secret, never commit
service-account.json
*-service-account.json

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# 원본 PDF는 Firebase Storage에 보관 — git엔 넣지 않음(용량). scratch는 렌더 임시물.
/docs/*.pdf
/scratch/
